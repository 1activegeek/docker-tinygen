FROM centos:centos8

# Options here to setup/enable a Web UI for configurations?
EXPOSE 

# Lets consolidate the list of variables that may be used here that would be runtime - IPs, Hosts, etc
ENV 

RUN 

COPY root/ /
